```mermaid
graph TD
    A[Start] --> B{ป้อนชื่อนักศึกษา};
    B --> C{ป้อนคะแนน};
    C --> D{คะแนนเป็นตัวเลขหรือไม่?};
    D -- ใช่ --> E{คะแนนอยู่ในช่วง 0-100 หรือไม่?};
    D -- ไม่ใช่ --> F[แสดงข้อผิดพลาด: "กรุณาป้อนคะแนนเป็นตัวเลข"];

    E -- ใช่ --> G{คะแนน >= 80?};
    E -- ไม่ใช่ --> H[แสดงข้อผิดพลาด: "กรุณาป้อนคะแนนในช่วง 0-100"];

    G -- ใช่ --> I[เกรด = "A"];
    G -- ไม่ใช่ --> J{คะแนน >= 70?};

    J -- ใช่ --> K[เกรด = "B"];
    J -- ไม่ใช่ --> L{คะแนน >= 60?};

    L -- ใช่ --> M[เกรด = "C"];
    L -- ไม่ใช่ --> N{คะแนน >= 50?};

    N -- ใช่ --> O[เกรด = "D"];
    N -- ไม่ใช่ --> P[เกรด = "F"];

    I --> Q[แสดงผลลัพธ์: ชื่อ, คะแนน, เกรด];
    K --> Q;
    M --> Q;
    O --> Q;
    P --> Q;

    F --> R[End];
    H --> R;
    Q --> R;
```
